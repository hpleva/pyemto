Installation
============

Install by running

.. sourcecode:: bash

   pip install pyemto

or

.. sourcecode:: bash

   git clone https://github.com/hpleva/pyemto.git
   cd pyemto
   python setup.py install


**Optional**: Try running some of the scripts from the :ref:`examples <examples-page>` page to verify the integrity of your pyEMTO installation.
